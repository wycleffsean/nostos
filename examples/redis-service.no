let
  app:
    name: "redis"
    port: 6379
in
apiVersion: "v1"
kind: "Service"
metadata:
  name: app.name
spec:
  type: "LoadBalancer"
  selector:
    app: app.name
  ports:
    - port: app.port
      targetPort: app.port
